lib.addCommand('aop', {
    help = 'sets AOP',
    params = {
        {
            name = 'location',
            type = 'string',
            help = 'location for AOP'
        }
    },
    restricted = true
}, function(source, args, raw)

end)
